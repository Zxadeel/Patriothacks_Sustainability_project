<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patriothackssus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PatriotHacksSus">
        <activity
            android:name=".ANIMATION_TEST"
            android:exported="false" />
        <activity android:name=".FailActivity"
        android:exported = "false"
        android:label = "FailActivity"
        android:theme="@style/Theme.PatriotHacksSus.NoActionBar" />
        <activity
            android:name=".SUCC"
            android:exported="false"
            android:label="@string/title_activity_succ"
            android:theme="@style/Theme.PatriotHacksSus.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>